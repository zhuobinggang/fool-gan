# fool-gan(Make GAN easily!)

```
from dcgan import *
m = DCGAN()
train_vanilla(m, epoch=60) # Will auto output 60 images to current folder
G.plot_random(m)
```

## Result(DCGAN)

![animated](anima.gif)
